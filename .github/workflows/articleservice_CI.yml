name: Article Service CI

on:
  push:
    branches:
      - main
    paths:
      - 'ArticleService/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.9' 

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r ArticleService/requirements.txt

    - name: Set PYTHONPATH
      run: echo "PYTHONPATH=$PYTHONPATH:$(pwd)/ArticleService" >> $GITHUB_ENV

    - name: Set up GCP credentials
      run: |
        echo "$GCP_FIRESTORE_CREDENTIALS" > gcp_credentials.json
        echo "GOOGLE_APPLICATION_CREDENTIALS=$(pwd)/gcp_credentials.json" >> $GITHUB_ENV

    - name: Print CI variable
      run: |
            echo $GOOGLE_APPLICATION_CREDENTIALS

    # - name: Run unit tests
    #   run: |
    #     pytest ArticleService/tests/unit
    #   env:
    #     GOOGLE_APPLICATION_CREDENTIALS: ${{ secrets.GCP_FIRESTORE_CREDENTIALS }}

    # - name: Run integration tests
    #   run: |
    #     pytest -m integration ArticleService/tests/test_mtg_article_pytest.py

  # Uncomment the following section when you have performance tests ready to be included
  # - name: Run performance tests
  #   run: |
  #     pytest -m performance ArticleService/tests/your_performance_tests_file.py
